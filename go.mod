module github.com/gearsdatapacks/dpm

go 1.20

// For testing only, must remove on release
replace github.com/gearsdatapacks/gorinth v0.1.0 => ../gorinth

require github.com/gearsdatapacks/gorinth v0.1.0
